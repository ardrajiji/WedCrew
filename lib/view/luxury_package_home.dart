import 'package:flutter/material.dart';

class LuxuryPackageHome extends StatelessWidget {
  const LuxuryPackageHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}