import 'package:flutter/material.dart';

class VendorBooking extends StatelessWidget {
  const VendorBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}